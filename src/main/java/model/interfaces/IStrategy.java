package model.interfaces;

/**
 *     This is Strategy Pattern Interface allow to mouse mode switching
 *     CreateCommand, SelectCommand and MoveCommand implement it
 */
public interface IStrategy {
    void run();
}
